# React Learning

Цей проєкт створений для поступового вивчення **React** — бібліотеки для
побудови інтерфейсів користувача.  
Мета — крок за кроком пройти шлях від базового JSX до побудови повноцінного
компонентного застосунку.

---

## ⚙️ Технології

- ⚛️ **React 19.1.1**
- 🧩 **TypeScript**
- ⚡ **Vite**
- 🎨 **CSS Modules**
- 🧱 Структурований підхід із гілками:  
  `main` → `stage-01-jsx` → `stage-02-components` → ...

---

## 📘 Stage 01 — JSX

### 🎯 Мета

Опанувати синтаксис **JSX**, навчитися використовувати його в компонентах React.

### 🧩 Основні поняття

- JSX — це розширення синтаксису JavaScript, що дозволяє писати HTML-подібний
  код.
- JSX перетворюється у виклики `React.createElement()`.
- У JSX можна вбудовувати змінні, вирази та виклики функцій у фігурних дужках
  `{}`.

### 🧠 Приклад

```tsx
const name = 'Віктор';
const element = <h1>Привіт, {name}!</h1>;
```

### 📄 Практичне завдання

Створити простий додаток із компонентом `App`, який відображає заголовок і
вітання користувача.

**Результат:**

- створено JSX-елементи,
- підключено стилі,
- використано структуру компонентів.

---

## 📘 Stage 02 — Components

### 🎯 Мета

Зрозуміти, що таке **компонент**, як він працює, і як компоненти взаємодіють між
собою через **props**.

### 🧩 Основні поняття

- Компоненти — це будівельні блоки React-застосунку.
- Вони можуть бути **функціональними** або **класовими** (у сучасному React —
  майже завжди функціональні).
- Дані передаються через **props**.
- Компоненти повинні повертати **один кореневий елемент** (можна обгорнути у
  `<>...</>`).

### 🧠 Приклад

```tsx
type HeaderProps = { title: string };

export default function Header({ title }: HeaderProps) {
  return (
    <header>
      <h1>{title}</h1>
    </header>
  );
}
```

### 📄 Практичне завдання

Створити три компоненти:

- `Header` — приймає `title`
- `Main` — приймає `content`
- `Footer` — приймає `year`

Підключити їх у `App.tsx` і відрендерити через `main.tsx`.

---

## ✅ Результат

🔹 Створено робочий React-застосунок.  
🔹 Налаштовано базову структуру з компонентами.  
🔹 Розібрано JSX, props і рендер компонентів.  
🔹 Підготовлено основу для наступного етапу — **Props і State у React**.

---

## 📂 Поточна структура проєкту

```
src/
 ├─ components/
 │   ├─ App.tsx
 │   ├─ Header.tsx
 │   ├─ Main.tsx
 │   └─ Footer.tsx
 ├─ styles/
 │   ├─ index.css
 │   └─ reset.css
 └─ main.tsx
```

---

## 🔀 Поточна гілка

**main** ← змержено `stage-01-jsx` та `stage-02-components`.

---

## 📘 Stage 03 — Props у React

### 🎯 Мета

Розібрати механізм **передачі даних через props** і створити компонент, який
отримує контент динамічно.

### 🧩 Основні поняття

- `props` — це вхідні параметри компонента (аналог аргументів функції).
- Через `props` можна передавати як окремі значення, так і JSX-елементи.
- В React `props` є **незмінними** (immutable).

### 🧠 Приклад реалізації

#### Компонент `Card`

```tsx
type CardProps = {
  children: React.ReactNode;
};

export default function Card({ children }: CardProps) {
  return <div className="card">{children}</div>;
}
```

#### Компонент `CardList`

```tsx
import Card from './Card';

const cards = [
  {
    title: 'Компоненти в React',
    text: 'Це незалежні частини інтерфейсу, які можна повторно використовувати.',
  },
  {
    title: 'Композиція',
    text: 'Дає змогу комбінувати компоненти для створення складніших UI-структур.',
  },
  {
    title: 'Вкладені компоненти',
    text: 'Компоненти можуть містити інші компоненти.',
  },
];

export default function CardList() {
  return (
    <section>
      {cards.map((item, index) => (
        <Card key={index}>
          <h2>{item.title}</h2>
          <p>{item.text}</p>
        </Card>
      ))}
    </section>
  );
}
```

#### Головний компонент `Main`

```tsx
import CardList from './CardList';

export default function Main() {
  return (
    <main>
      <CardList />
    </main>
  );
}
```

### 📄 Практичне завдання

1. Створити компонент `Card`, який приймає контент через `props.children`.
2. Створити компонент `CardList`, який відображає кілька `Card` на основі масиву
   даних.
3. Відрендерити все через компонент `Main`.

---

## ✅ Результат

🔹 Реалізовано компонент `Card` з використанням `props.children`.  
🔹 Створено компонент `CardList`, який динамічно формує вміст із масиву.  
🔹 Підготовлено структуру для переходу до наступної теми — **стан компонентів
(state)**.

---

## 📂 Поточна структура проєкту

```
src/
 ├─ components/
 │   ├─ App.tsx
 │   ├─ Header.tsx
 │   ├─ Main.tsx
 │   ├─ Footer.tsx
 │   ├─ Card.tsx
 │   └─ CardList.tsx
 ├─ styles/
 │   ├─ index.css
 │   └─ reset.css
 └─ main.tsx
```

---

## 🔀 Поточна гілка

**main** ← змержено `stage-01-jsx`, `stage-02-components` та `stage-03-props`.

---

## 🚀 Далі

➡️ **Stage 04 — State у React**  
Вивчимо:

- локальний стан за допомогою `useState()`;
- керування станом у списках і формах;
- передавання функцій як props для оновлення стану.

---

© 2025 React Learning Project — навчальний репозиторій
